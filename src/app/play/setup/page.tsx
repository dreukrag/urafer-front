// `app/page.tsx` is the UI for the `/play/setup` URL
export default function SetupPage() {
  return (
    <h1>
      This is the setup area. There should be stuff like a list letting you
      choose a character to play as. Selecting one of them should send you back
      to the &rdquo;/play&rdquo; page
    </h1>
  );
}
